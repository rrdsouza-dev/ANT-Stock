Subir server uvcorn Back-end
uvicorn main:app --reload

subir server uvcorn Front-end (public)
python -m http.server 5500
http://localhost:5500





passar do login
rodar no console
localStorage.removeItem("antstock:session");
localStorage.removeItem("antstock:session:v2");

localStorage.setItem(
  "antstock:session:v2",
  JSON.stringify({
    user: {
      id: 1,
      nome: "Administrador",
      profile: "adm"
    },
    token: "dev-token",
    depositId: 1
  })
);

localStorage.setItem(
  "antstock:session",
  JSON.stringify({
    authenticated: true,
    token: "dev-token",
    user: {
      id: 1,
      nome: "Administrador",
      profile: "adm"
    }
  })
);

location.reload();
